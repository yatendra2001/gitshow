node_modules
.next
.open-next
.wrangler
.vercel
*.tsbuildinfo

# Local-dev-only secrets. `.dev.vars.example` IS committed as a
# template; the filled `.dev.vars` is not.
.dev.vars

# cloudflare-env.d.ts is hand-maintained for now (not regenerated by
# wrangler types). Commit it so CI typechecks see the bindings.

# Env
.env
.env.local
.env.*.local

# OS / editors
.DS_Store
*.log
