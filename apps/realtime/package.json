{
  "name": "@gitshow/realtime",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Realtime fan-out for pipeline events. Hosts ScanLiveDO — a Durable Object per scan that accepts POST /events from the Fly worker and fans them out to connected browsers over hibernatable WebSockets.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@gitshow/shared": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260418.1",
    "@types/node": "^22.10.0",
    "typescript": "^5.6.3",
    "wrangler": "^4.0.0"
  }
}
