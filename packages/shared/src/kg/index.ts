/**
 * KG public surface.
 *
 * Import via `@gitshow/shared/kg`. The schema lives in `./schema.ts`,
 * the fact union in `./typed-fact.ts`, and ID derivation in `./slug.ts`.
 */

export * from "./schema.js";
export * from "./typed-fact.js";
export * from "./slug.js";
